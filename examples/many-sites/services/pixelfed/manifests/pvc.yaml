apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pixelfed-storage
  labels:
    app.kubernetes.io/name: pixelfed
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .storageClass }}
  resources:
    requests:
      storage: {{ .storage.pixelfed }}
