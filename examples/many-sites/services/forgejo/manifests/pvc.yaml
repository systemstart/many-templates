apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: forgejo-data
  labels:
    app.kubernetes.io/name: forgejo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .storageClass }}
  resources:
    requests:
      storage: {{ .storage.forgejo }}
