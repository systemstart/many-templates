apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mobilizon-uploads
  labels:
    app.kubernetes.io/name: mobilizon
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .storageClass }}
  resources:
    requests:
      storage: {{ .storage.mobilizon }}
